# Ant build properties

verbose.mode=off

app.name=PhysicsDemos
app.version=0.03
app.author=Ethan Blackwelder
app.website=https://github.com/eib/java-physics-demos

src.home=${basedir}${file.separator}src
demos.home=${basedir}${file.separator}demos
lib.home=${basedir}${file.separator}lib

build.home=${basedir}${file.separator}build
dist.home=${basedir}${file.separator}dist

jar.name=${app.name}-${app.version}.jar
jar.target=1.6
jar.main-class=com.eblackwelder.physics.PhysicsMain

run.main-class=${jar.main-class}
